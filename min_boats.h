#ifndef MIN_BOATS_H

#define MIN_BOATS_H

#include <vector>

// Function declarations (only the function names and parameters, not the logic)
int min_boats(std::vector<int>& people, const int& limit);

#endif